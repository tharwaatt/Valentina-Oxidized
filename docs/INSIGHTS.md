# 📓 مفكرة الرؤى والدروس المستفادة | INSIGHTS.md

هذا الملف هو السجل الهندسي لمشروع **Valentina-Oxidized**. هنا نسجل التحديات التقنية، الفخاخ التي واجهتنا، ولماذا اتخذنا قرارات معمارية معينة.

---

## 🛑 الفخاخ التقنية (Technical Pitfalls)

### 1. معضلة الإحداثيات (The Coordinate Mismatch)
* **المشكلة:** نقاط الماوس تظهر بعيدة عن مكان النقر الحقيقي في الـ SVG.
* **الرؤية:** إحداثيات الماوس التي يعطيها المتصفح هي بكسلات CSS، بينما الـ SVG يستخدم نظام إحداثيات `viewBox` منطقي.
* **الحل:** بناء `CoordMapper` يقوم بتحويل رياضي دقيق يعتمد على نسبة العرض والارتفاع (`AspectRatioMode`).

### 2. تجميد الذاكرة (The Signal Deadlock)
* **المشكلة:** تجمد البرنامج فجأة عند محاولة إضافة عنصر جديد أثناء الرسم.
* **الرؤية:** في Dioxus 0.7، محاولة الحصول على قفل كتابة (`.write()`) في مخزن البيانات أثناء وجود قفل قراءة (`.read()`) نشط داخل حلقة الرسم تؤدي لـ Deadlock.
* **الحل:** استخدام تقنية **Snapshotting**؛ أخذ نسخة كاملة (`clone()`) من البيانات قبل البدء في حلقة الرسم لتحرير أقفال الذاكرة فوراً.

---

## 🛠️ قرارات معمارية (Architectural Decisions)

### 1. فلسفة الربط بالمعرفات (ID-based Linking)
* **القرار:** عدم استخدام مراجع Rust (`&`) للربط بين النقاط والخطوط، والاعتماد الكلي على `u32 IDs`.
* **الفائدة:** سمح لنا هذا القرار بتنفيذ **Interactive Drag & Drop** بسهولة تامة؛ حيث أن الخط لا يحتاج لتغيير مراجعة، بل يقوم ببساطة بسحب الإحداثيات الجديدة للنقطة عبر معرفها في كل فريم رسم.

### 2. التجميع المنطقي (Path Consolidation)
* **القرار:** إنشاء هيكل `VContour` يحمل "مراجع كيانات" (`EntityRef`) بدلاً من نسخ البيانات.
* **السبب:** الحفاظ على "مصدر وحيد للحقيقة" (Single Source of Truth). إذا تحركت نقطة، يتحرك الخط، ويتحدث المسار (Path) المجمع تلقائياً.

### 3. الحذف المتتالي (Cascading Deletion)
* **القرار:** تنفيذ منطق حذف الأبناء عند حذف الآباء برمجياً.
* **الفائدة:** حماية قاعدة البيانات الرسومية من العناصر "اليتيمة" (Orphaned Entities) التي كانت ستسبب انهيار البرنامج عند محاولة الاستعلام عن نقاط غير موجودة.

---

## 💡 تأملات في لغة Rust والـ UI

* **Reactive State Machine:** تعلمت أن إدارة واجهة CAD معقدة تتطلب "آلة حالة" (State Machine) صريحة (`CanvasMode`) لتبديل السلوك البرمجي للنقرات.
* **Serde is Magic:** تحويل مشروع CAD كامل إلى JSON واسترجاعه أصبح يتم في أسطر معدودة بفضل قوة التجريد في مكتبة `Serde`.
* **Zero-Cost Path Drawing:** الاعتماد على `SVG Path (C command)` لرسم منحنيات بيزيه وفر علينا مئات الأسطر من كود C++ المعقد لتقسيم المنحنيات.

---

### 📝 سجل التحديثات:

* **[2026-02-17]**: تحديث المرحلة الثالثة: حلول الـ Deadlock، تحويل الإحداثيات، وفلسفة الـ Drag & Drop التفاعلي.
* **[2026-02-15]**: إضافة دروس اليوم الرابع (تثبيت مكتبات Linux والتعامل مع خصائص SVG في Dioxus).

---
