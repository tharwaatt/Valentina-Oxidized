# Day 4: From Math to Motion (Dioxus GUI) 🎨

في هذا اليوم، انتقلنا من مرحلة "الأكواد الصامتة" إلى مرحلة "الواجهة الحية". لم تعد النقطة مجرد أرقام في الذاكرة، بل أصبحت كياناً مرئياً يتفاعل مع الماوس ويتحرك على الشاشة.

---

## 1. فلسفة الواجهة: لماذا Dioxus و SVG؟

بدلاً من بناء واجهة تقليدية، قررنا استخدام نهج **هجين**:

* **Dioxus:** لسهولة بناء الواجهات وتحديثها تلقائياً عند تغيير البيانات (State Management).
* **SVG (Scalable Vector Graphics):** لأنه اللغة الطبيعية لبرامج الـ CAD؛ فالخطوط والدوائر فيه ليست بكسلات، بل معادلات رياضية تحافظ على دقتها مهما قمت بعمل Zoom.

---

## 2. إدارة الحالة: الـ Signal والمخزن المركزي

بدلاً من التعامل مع متغيرات عادية، استخدمنا الـ **Signals**:

* `use_signal(|| Vec::<VPoint>::new())`: هذا السطر أنشأ مخزناً مركزياً حياً. بمجرد أن نضيف نقطة لهذا الـ Vector، يقوم Dioxus بإعادة رسم الواجهة فوراً دون تدخل منا.

---

## 3. تشريح ملف `src/main.rs` (النسخة التفاعلية)

هنا قمنا بدمج كل ما تعلمناه في الأيام السابقة:

### أ. التقاط إحداثيات الماوس

استخدمنا حدث الـ `onclick` لتحويل "لمسة" المستخدم إلى "نقطة هندسية":

```rust
onclick: move |evt| {
    let coords = evt.element_coordinates(); // تحويل إحداثيات الشاشة إلى إحداثيات الرسم
    let new_point = VPoint::new(id, "P1", coords.x, coords.y);
    points.with_mut(|p_vec| p_vec.push(new_point));
}

```

### ب. الرسم الذكي باستخدام Loops

داخل الـ SVG، قمنا بتمثيل النقاط برمجياً:

```rust
for p in points().iter() {
    circle { cx: "{p.x()}", cy: "{p.y()}", r: "6", fill: "red" }
}

```

---

## 4. التحديات التقنية (The Hard Way)

لم يكن الطريق سهلاً، واجهنا عقبتين كبيرتين:

### أ. معركة مكتبات Linux (Linker Error)

اكتشفنا أن Rust يحتاج للتحدث مع نظام التشغيل لفتح نافذة.

* **الخطأ:** `unable to find library -lxdo`.
* **الحل:** تثبيت حزم التطوير للنظام مثل `libxdo-dev` و `libwebkit2gtk-4.1-dev`. هذا علمنا أن برمجة الـ Desktop تتطلب فهماً لمكتبات نظام التشغيل.

### ب. قواعد تسمية SVG في Rust

تعلمنا أن Dioxus يلتزم بمعايير Rust الصارمة:

* الخصائص مثل `patternUnits` في الـ HTML يجب أن تكتب `pattern_units` (Snake Case) في Rust.
* المترجم (Compiler) كان بمثابة "معلم" يصحح لنا هذه المسميات فوراً.

---

## 5. الدروس المستفادة من Day 4 (The Interaction Leap)

* **المترجم كمساعد:** تعلمنا قراءة أخطاء الـ `Linker` والبحث عن الحزم المفقودة في نظام التشغيل.
* **فصل المنطق عن العرض:** بفضل فصل `geometry.rs` في الأيام السابقة، كان رسم النقطة في الواجهة سهلاً جداً لأننا استدعينا دوال جاهزة ومجربة.
* **تجربة المستخدم (UX):** إضافة شبكة (Grid) خلفية جعلت المستخدم يشعر بمقياس الرسم، وهي أول خطوة في تحويل البرنامج لأداة هندسية حقيقية.

---