# Stage 2: Core GUI & Identity 🎨📑

في هذه المرحلة، انتقلنا من الأكواد الرياضية "الصامتة" إلى بناء كائنات لها "هوية" وتطوير واجهة رسومية تفاعلية تسمح للمصمم بالتعامل مع النظام بشكل مرئي.

## جدول المحتويات
1. [Core Identity: تعريف هيكل الـ VGObject](#هوية-الكائن)
2. [Dioxus Integration: لماذا الويب لبرنامج Desktop؟](#واجهة-dioxus)
3. [تحديات النظام: معركة مكتبات Linux والـ Linker](#تحديات-النظام)
4. [إدارة الحالة: أول لقاء مع الـ Signals](#إدارة-الحالة)

---

## 1. Core Identity: الهوية والتركيب {#هوية-الكائن}

في برامج الـ CAD، النقطة ليست مجرد أرقام (x, y)، بل هي كائن له اسم (مثل "A1") ورقم تعريف (ID) فريد وحالة رسم (مثل "مخفي" أو "ظاهر").

قمنا ببناء هيكل `VGObject` ليكون "بطاقة الهوية" لكل شيء في Valentina:
```rust
pub struct VGObject {
    pub id: u32,
    pub name: String,
    pub obj_type: GOType,
    pub mode: DrawMode,
}
```
**لماذا استخدمنا `String` بدلاً من `&str`؟**
داخل الهياكل (Structs)، يفضل Rust أن "تمتلك" الهوية بياناتها. لو استخدمنا `&str` (مرجع مؤقت)، لدخلنا في تعقيدات الـ Lifetimes التي ستجعل تحريك النقاط أو تعديلها مستحيلاً برمجياً في هذه المرحلة.

---

## 2. Dioxus Integration: الواجهة الحية {#واجهة-dioxus}

اتخذنا قراراً استراتيجياً باستخدام **Dioxus** مع **SVG**:
- **SVG (Scalable Vector Graphics):** هي اللغة الطبيعية للـ CAD. الخطوط فيها ليست بكسلات، بل معادلات. هذا يضمن أن التصميم سيظل دقيقاً مهما قمنا بعمل Zoom.
- **التفاعلية:** بمجرد أن يضغط المستخدم على الشاشة، نريد للنقطة أن تظهر فوراً. Dioxus وفر لنا نظام "تفاعلي" يشبه React ولكن بقوة Rust.

---

## 3. تحديات النظام: معركة مكتبات Linux والـ Linker {#تحديات-النظام}

برمجة تطبيقات الـ Desktop في Rust تتطلب التحدث مع نظام التشغيل. حللنا المشكلة بتثبيت حزم التطوير:
- `libxdo-dev`: للتعامل مع أحداث الماوس ولوحة المفاتيح.
- `libwebkit2gtk-4.1-dev`: لتشغيل محرك الويب الذي يعرض واجهة Dioxus.
---

## 4. إدارة الحالة: أول لقاء مع الـ Signals {#إدارة-الحالة}

كيف نخزن النقاط بحيث تظهر في الواجهة فور إنشائها؟ استخدمنا الـ **Signals**:
`let mut points = use_signal(|| Vec::<VPoint>::new());`

الـ Signal هو "مخزن ذكي". عندما نضيف نقطة جديدة باستخدام `points.write().push(new_p)`، يقوم Dioxus تلقائياً بمعرفة أن البيانات تغيرت، فيعيد رسم الـ SVG فوراً دون أن نطلب منه ذلك يدوياً. هذا هو سحر "البرمجة التفاعلية" (Reactive Programming).

### الدروس التقنية المستفادة:
- **إحداثيات العنصر:** استخدمنا `element_coordinates()` لتحويل ضغطة الماوس إلى موقع حقيقي داخل الـ SVG.
- **الشبكة (Grid):** أضفت شبكة خلفية برمجياً باستخدام `pattern` في الـ SVG لتعطي المصمم إحساساً بالمقاييس الحقيقية.

**الخلاصة:** المرحلة الثانية حولت "المعادلات الصماء" إلى "كيانات مرئية". أصبح لدينا الآن "ملعب" هندسي يمكننا البناء فيه.
