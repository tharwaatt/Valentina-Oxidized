# Stage 2: Core GUI & Identity 🎨📑

في هذه المرحلة، انتقلنا من الأكواد الرياضية الصامتة إلى بناء "هوية" للكائنات وتطوير واجهة رسومية تفاعلية.

## جدول المحتويات
1. [Core Identity: من أنا؟ (Composition over Inheritance)](#هوية-الكائن)
2. [Dioxus Integration: بناء الواجهة الحية](#واجهة-dioxus)
3. [التحديات التقنية وإدارة الحالة](#تحديات-الحالة)

---

## 1. Core Identity: الهوية والتركيب {#هوية-الكائن}

بدلاً من جعل النقطة ترث خصائص الكائن (كما في C++)، استخدمنا **التركيب (Composition)**:
- النقطة "تمتلك" هوية (`VGObject`) وتحتوي على إحداثيات (`Point2D`).

```rust
pub struct VGObject {
    pub id: u32,
    pub name: String,
    pub obj_type: GOType,
    pub mode: DrawMode,
}
```

**لماذا String؟** لأن الـ Struct يجب أن "يمتلك" بياناته لضمان بقائها في الذاكرة طوال فترة حياة الكائن.

---

## 2. Dioxus Integration: الواجهة الحية {#واجهة-dioxus}

استخدمنا إطار عمل **Dioxus** مع **SVG** لرسم الأشكال الهندسية:
- **SVG:** اللغة الطبيعية للـ CAD لأنها تعتمد على المعادلات وليس البكسلات.
- **Signals:** تحديث تلقائي للواجهة بمجرد تغيير البيانات.

```rust
// رسم النقاط برمجياً داخل SVG
for p in points().iter() {
    circle { cx: "{p.x()}", cy: "{p.y()}", r: "6", fill: "red" }
}
```

---

## 3. التحديات التقنية وإدارة الحالة {#تحديات-الحالة}

### معركة مكتبات Linux (Linker Error)
برمجة الـ Desktop في Rust تتطلب مكتبات نظام التشغيل:
- تم حل مشكلة `unable to find library -lxdo` عبر تثبيت `libxdo-dev` و `libwebkit2gtk-4.1-dev`.

### تجربة المستخدم (UX)
- إضافة **SVG Grid** خلفية لإعطاء إحساس بمقياس الرسم.
- استخدام `element_coordinates()` لتحويل ضغطات الماوس إلى نقاط هندسية دقيقة.

**الخلاصة:** في نهاية المرحلة الثانية، أصبح لدينا محرك قادر على عرض البيانات والتفاعل مع المستخدم بفضل فصل المنطق (Logic) عن العرض (View).
